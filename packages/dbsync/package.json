{
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "name": "@fg/dbsync",
  "version": "1.0.0",
  "dependencies": {
    "@fg/utils": "1.0.0",
    "nodemon": "^2.0.15",
    "pg": "^8.7.1",
    "sequelize": "^6.12.0-beta.1",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "build": "tsc --build --force tsconfig.json",
    "app": "node dist",
    "app-watch": "nodemon",
    "app-dev": "nodemon -L --config nodemon-debug.json",
    "app-brk": "nodemon --config nodemon-debug-brk.json"
  }
}
